<%= form_for(@membertype) do |f| %>
  <% if @membertype.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@membertype.errors.count, "error") %> prohibited this membertype from being saved:</h2>

      <ul>
      <% @membertype.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
